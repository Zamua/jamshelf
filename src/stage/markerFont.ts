import markerFontUrl from './PermanentMarker-Regular.ttf';

// The shelf paper-label font: a marker hand (Permanent Marker, OFL). Bundled locally so
// the drei <Text> never fetches a fallback font from a CDN.
export const MARKER_FONT: string = markerFontUrl;
